cursortalk to figma mcp 깃허브가서 다운로드 받았다.
받아서 압축푼다.
폴더를 커서에 드랍한다.
연결되었으면
터미널에서 저 좆같은 번깔기,번세텁,웹소켓서버시작,
이 되어야하는데 윈도우 환경이므로
윈도우에서 bash가 지원하게끔 만들어서(윈도우 섭시스템 포
리눅스)를 깔고 실행하는데,

이 모든걸 커서가 알아서 할 수 있다. 
그러니까. 메뉴얼을 쭉 드래그해서 커서한테 먹이고 알아서
하라고 한다.(됐다)
그다음.
웹소켓의 번호를 물어봤다. (3055번이라고 했음)
내 쪽에선 연결성공메세지가 커서쪽에 보이지는 않았다.
아마 커서가 모든일을 처리하는 과정중에 allow 만 반복하다가
넘어간거 같다.

새로운 mcp가 감지되었다는 말이 왜 안나오는냐고 물어봤고
자기가 알아서 내부를 정리한 뒤, 완전한 재시작을 요구했음.
재시작 하고 나서,
결론적으로 커서쪽에선 커서한테 웹소켓서버시작 되었냐 
소켓번호뭐냐고 물어보고 확인했음.
커서는  talk to Figma MCP가 감지되었다고 함.

여기서 피그마 데스트탑으로 이동한다.
화면 우하단 플러그인 아이콘 선택.
plug in & widget - all 에서 cursor을 쓰고 휠을 내리면,
talk to Figma MCP 가 나온다.
이게 연결되어있으면 웹소켓번호와 채널명이 나온다.
커서한테 이 정보 준다. 연결하라고 시킨다.

연결성공.





