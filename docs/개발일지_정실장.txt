.log
개발일지

캐릭터의 달리는 모습의 도트 원화를 지피티로 그렸다.
그록으로 애니메이션을 만들었다.
배경 크로마키를 위해 우선 배경을 라임오렌지로 바꿨다.(나노바나나)
적용에는 성공했다.
그런데 커서의 말에 따르면, 서버를 띄운다음 (내 컴퓨터가 서버가 되는 보양이다) 웹 브라우저에서 실행해야 크로마키가 먹는다고 해면서
실행방법을 알려주었다.
그래서 시킨대로 했다.
커서의 말에 따르면 파이썬 언어를 이용하여 커서 터미널에서 서버를 띄웠다.(docs에 기록이 있음)
결론적으로 크로마키한 캐릭터 도트  애니메가 게임에 등장하게 까지 하는데 성공, 이게 어떻게든 웹 상에서 나오게 하고싶음,

폭탄, 이단점프, 효과음, 클리어연출등 자잘한거 넣었음 

1월28일
**로컬서버열기
로컬서버열기 (이거그냥 무지성 매일 반복해라)
'프로젝트 폴더'로 가서 
빈 공간에서 쉬프트 오른클릭. 빠와 쉘 열기,

npx.cmd -y serve . -l 3000

입력. npx는 무슨 권한이 어쩌고를 우회한다고 했음


커서에 mcp이어보는 연습을 했음.playwrite 시도성공
픽셀그래픽 생성 mcp연결은 유료 베어러 토큰문제로 중단.
도트 그래픽 애니메이션을 생성하는 mcp따위는 없다.
ai로 동영상까지 만들고 프레임을 추출하여 
가로로긴 그림을 하나 만든다음, 이걸 Asprite로 가져가기로 했다.

영상에서 프레임을 추출하는 도구를 받았다
ffmpeg 라는 도구임
F:\cursor_project\ffmpeg-2026-01-29-git-c898ddb8fe-essentials_build\ffmpeg-2026-01-29-git-c898ddb8fe-essentials_build\bin
까지 들어와서 같은 폴더에 리소스영상 (run.mp4으로 시험함)를 두고
그 폴더의 주소창에 cmd를 입력한 다음
프레임 추출명령어 ffmpeg -i run.mp4 frame_%03d.png 입력한다.

*ffmpeg 프레임 추출명령어
ffmpeg -i shoot.mp4 frame_%03d.png

ffmpeg -i bomb.mp4 frame_%03d.png


추출한 프레임을바탕으로 사용할 프레임을 모은다.
 Asprite에서 파일 - new - sprite로 스프라이트 시트를 하나연다.

****** Asprite는 파일의 이름이 시퀀셜하면 애니메이션 프레임으로
인식하는데, 하나의 시트에 이걸 애니메이션 프레임으로 소환하는 방법은 '오로지 시퀀셜한 파일이름뿐'이다. *****파일 이름을 시퀀셜하게
저장해야 한다!!!!!

암튼 모두 열고 이 각 프레임을 아까 열었던 스프라이트 시트지에 다 때려넣는다. 알트 + N으로 프레임을 추가한다. 가진 프레임수만큼 추가하고 프레임 전체선택 카피 - 스프라이트 시트상 지정 프레임에 복붙한다.












